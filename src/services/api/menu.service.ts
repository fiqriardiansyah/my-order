import type { MenuInput } from "../interfaces/menu.interface";

// Stub — implement these when the NestJS API is ready.

export async function createMenu(
  _restaurantId: string,
  _input: MenuInput
): Promise<void> {
  throw new Error("API menu service: not implemented");
}
